kind = "web"
previewPath = "/"
title = "Poke Haven"
version = "1.0.0"
id = "artifacts/poke-haven"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25902

[services.development]
run = "pnpm --filter @workspace/poke-haven run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/poke-haven", "run", "build" ]
publicDir = "artifacts/poke-haven/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25902"
BASE_PATH = "/"

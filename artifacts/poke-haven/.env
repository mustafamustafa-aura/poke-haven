# ─── Frontend (Netlify/Vercel build env) ─────────────────────────────────────
# Leave empty when /api is proxied to your backend on the same host.
VITE_API_URL=http://localhost:8080

# Admin dashboard key (sent as X-Admin-Key from /admin — optional in local dev)
# VITE_ADMIN_HINT=set-same-as-ADMIN_API_KEY-for-staff

# Vite dev/build (defaults: PORT=5173, BASE_PATH=/)
# PORT=5173
# BASE_PATH=/

# ─── API server (Railway, Render, Fly.io, etc.) ──────────────────────────────
PORT=8080
DATABASE_URL=postgresql://user:password@localhost:5432/poke_haven

# Required in production for GET/PATCH /api/orders (admin dashboard)
ADMIN_API_KEY=mustafa2026

# Comma-separated allowed browser origins (optional; default allows all)
# CORS_ORIGIN=https://your-site.netlify.app,http://localhost:5173

# LOG_LEVEL=info
# NODE_ENV=production

# ─── Netlify (Site settings → Environment) ─────────────────────────────────────
# API_URL=https://your-api.example.com
# (Used in netlify.toml redirect: /api/* → API_URL)
VITE_SUPABASE_URL=https://khqqvgfsadpoqnzuzwdf.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_dT3yoddLuKSRfNhOr6VI_Q_ZjoOia5A